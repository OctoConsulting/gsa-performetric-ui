import { OnInit, TemplateRef } from '@angular/core';
import { BehaviorSubject } from "rxjs";
import { SearchListInterface, SearchListConfiguration } from './model/search-list-layout.model';
import { SDSFormlyUpdateComunicationService } from '@gsa-sam/sam-formly';
export declare class SearchListLayoutComponent implements OnInit {
    private formlyUpdateComunicationService;
    /**
    * Child Template to be used to display the data for each item in the list of items
    */
    resultContentTemplate: TemplateRef<any>;
    constructor(formlyUpdateComunicationService: SDSFormlyUpdateComunicationService);
    /**
     * Input service to be called when items change
     */
    service: SearchListInterface;
    /**
     * configuration
     */
    configuration: SearchListConfiguration;
    /**
     * Filter information
     */
    private filterData;
    ngOnInit(): void;
    /**
     * Default Page setttings
     */
    page: {
        pageNumber: number;
        pageSize: number;
        totalPages: number;
    };
    /**
     * updates the filter and set the page number to 1 and calls imported service
     * @param filter
     */
    updateFilter(filter: any): void;
    /**
     * Sorty by change event
     */
    onSelectChange(): void;
    /**
     * Id of the top pagination control
     */
    top: {
        id: string;
    };
    /**
     * Id of the bottom pagination control
     */
    bottom: {
        id: string;
    };
    /**
     * Page event listener
     */
    paginationChange: BehaviorSubject<object>;
    /**
     * List of items to be displayed
     */
    items: any[];
    /**
     * sort value
     */
    sortField: string;
    /**
     * calls service when updated
     */
    private updateContent;
}
