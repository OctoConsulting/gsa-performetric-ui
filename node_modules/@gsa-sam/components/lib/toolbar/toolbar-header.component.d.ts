import { SdsToolbarComponent } from "./toolbar.component";
export declare class SdsToolbarHeaderComponent {
    toolbar: SdsToolbarComponent;
    constructor(toolbar: SdsToolbarComponent);
    /** Toggles the expanded state of the toolbar. */
    _toggle(): void;
    /** Gets whether the toolbar is expanded. */
    readonly _isExpanded: boolean;
    /** Whether the toolbar is disabled. */
    readonly _disabled: boolean;
}
