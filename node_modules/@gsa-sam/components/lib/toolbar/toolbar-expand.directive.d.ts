import { ElementRef, Renderer2, OnInit } from "@angular/core";
import { SdsToolbarComponent } from "./toolbar.component";
export declare class SdsToolbarExpandDirective implements OnInit {
    private renderer;
    private _element;
    /** Width of the toolbar while expanded. */
    _expandedWidth: string;
    /** References the toolbar instance that the element its associated with. */
    toolbar: SdsToolbarComponent;
    private _toolbar;
    constructor(renderer: Renderer2, _element: ElementRef);
    ngOnInit(): void;
    private setStyle;
}
