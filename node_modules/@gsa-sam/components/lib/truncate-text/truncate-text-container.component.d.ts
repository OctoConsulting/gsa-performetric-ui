export interface SdsTruncateTextData {
    text: string;
}
export declare class SdsTruncatedTextContainerComponent {
    data: SdsTruncateTextData;
    _animationState: string;
    constructor(data: SdsTruncateTextData);
    /** Starts the animation. */
    startAnimation(): void;
    /** Resets the animation to its initial state. */
    resetAnimation(): void;
    /** Intentionally left empty to trigger change detection */
    _onAnimationDone(): void;
}
