import { OnChanges, OnInit, SimpleChanges, ElementRef } from '@angular/core';
import { SdsDialogService } from './dialog';
import { SdsDialogRef } from './dialog-ref';
/**
 * Button that will close the current dialog.
 */
export declare class SdsDialogCloseDirective implements OnInit, OnChanges {
    dialogRef: SdsDialogRef<any>;
    private _elementRef;
    private _dialog;
    /** Screenreader label for the button. */
    ariaLabel: string;
    /** Dialog close input. */
    dialogResult: any;
    _sdsDialogClose: any;
    constructor(dialogRef: SdsDialogRef<any>, _elementRef: ElementRef<HTMLElement>, _dialog: SdsDialogService);
    ngOnInit(): void;
    ngOnChanges(changes: SimpleChanges): void;
}
/**
 * Title of a dialog element. Stays fixed to the top of the dialog when scrolling.
 */
export declare class SdsDialogTitleDirective implements OnInit {
    private _dialogRef;
    private _elementRef;
    private _dialog;
    id: string;
    constructor(_dialogRef: SdsDialogRef<any>, _elementRef: ElementRef<HTMLElement>, _dialog: SdsDialogService);
    ngOnInit(): void;
}
/**
 * SubTitle of a dialog element
 */
export declare class SdsDialogSubtitleDirective {
}
/**
 * Scrollable content container of a dialog.
 */
export declare class SdsDialogContentDirective {
}
/**
 * Container for the bottom action buttons in a dialog.
 * Stays fixed to the bottom when scrolling.
 */
export declare class SdsDialogActionsDirective {
}
