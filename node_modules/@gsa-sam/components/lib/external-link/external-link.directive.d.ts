import { ElementRef, Renderer2, ComponentFactoryResolver, ViewContainerRef, OnChanges } from '@angular/core';
export declare class ExternalLinkDirective implements OnChanges {
    private el;
    private renderer;
    private cfr;
    private vc;
    private vcRef;
    href: string;
    hideIcon: boolean;
    private readonly isExternalLink;
    constructor(el: ElementRef, renderer: Renderer2, cfr: ComponentFactoryResolver, vc: ViewContainerRef);
    ngOnChanges(): void;
    private createIcon;
}
