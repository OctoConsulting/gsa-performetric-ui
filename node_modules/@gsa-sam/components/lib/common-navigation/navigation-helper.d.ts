import { INavigationLink } from './common-navigation-model';
export declare class NavigationHelper {
    /**
     * checks if link is internal
     * @param link
     */
    isLinkInternal(link: INavigationLink): boolean;
    /**
     * checks if link is external
     * @param link
     */
    isLinkExternal(link: INavigationLink): boolean;
}
