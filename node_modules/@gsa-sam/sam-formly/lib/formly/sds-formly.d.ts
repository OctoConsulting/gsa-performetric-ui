import { ChangeDetectorRef, DoCheck } from '@angular/core';
import { FieldType } from '@ngx-formly/core';
export declare abstract class AbstractSdsFormly extends FieldType implements DoCheck {
    cdr: ChangeDetectorRef;
    template: any;
    ngDoCheck(): void;
    setProperties(component: any, configuration: any): void;
}
