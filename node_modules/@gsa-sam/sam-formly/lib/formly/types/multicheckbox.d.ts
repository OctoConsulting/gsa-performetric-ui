import { FieldType } from '@ngx-formly/core';
export declare class FormlyFieldMultiCheckboxComponent extends FieldType {
    defaultOptions: {
        templateOptions: {
            options: any[];
        };
    };
    onChange(value: any, checked: boolean): void;
}
