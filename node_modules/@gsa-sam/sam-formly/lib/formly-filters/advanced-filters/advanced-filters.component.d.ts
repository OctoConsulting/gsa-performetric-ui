import { FormGroup } from '@angular/forms';
import { SdsDialogService } from '@gsa-sam/components';
import { FormlyFieldConfig, FormlyFormOptions } from '@ngx-formly/core';
import { SdsAdvancedFiltersService } from './sds-advanced-filters.service';
export declare class AdvancedFiltersComponent {
    dialog: SdsDialogService;
    private advancedFiltersService;
    /**
   * Pass in a Form Group for ReactiveForms Support
   */
    form: FormGroup;
    /**
     *  Fields are used to configure the UI components
     */
    fields: FormlyFieldConfig[];
    /**
     *  Model used to display the filter values.
     */
    model: any;
    /**
     *    Options for the form.
     */
    options: FormlyFormOptions;
    constructor(dialog: SdsDialogService, advancedFiltersService: SdsAdvancedFiltersService);
    openDialog(): void;
}
